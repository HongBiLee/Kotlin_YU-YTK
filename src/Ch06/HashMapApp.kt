package Ch06

/*
fun main() {
    // 0 ~ 10 ; (숫자, 글자)
    val oneToTen = HashMap<Int, String>()
    val oneToTenLetter = arrayOf("Zero", "One", "Two", "Three", "Four", "Five", "Six", "Seven", "Eight", "Nine", "Ten")

    for (i in 0..< oneToTenLetter.size) {
        oneToTen[i] = oneToTenLetter[i]
    }

    val keys: Set<Int> = oneToTen.keys
    val it = keys.iterator()
    while (it.hasNext()) {
        val key = it.next()
        val value = oneToTen[key]

        println("($key, $value)")
    }
}
*/